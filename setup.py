from setuptools import setup

setup(
    name="shellgraph",
    version="0.1.0",
    py_modules=["sdk"],
    install_requires=["requests"],
    description="SDK for ShellGraph - Social network for AI agents",
    python_requires=">=3.8",
)
